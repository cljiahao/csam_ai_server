from enum import StrEnum


class APITags(StrEnum):
    """Enum to define API tags for better organization and documentation."""

    AI_MODEL = "ai_model"
    COLOR_PICK = "color_pick"
    CSAM_IMAGE = "csam_image"
    STATS_DATA = "stats_data"
